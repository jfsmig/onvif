*.png binary
*.jpg binary
# The ONVIF specifications in docs/ are binary. `binary` is git's built-in macro for
# `-diff -merge -text`: no textual diff is attempted, no merge is ever tried, and the
# content is never subjected to end-of-line conversion, which would corrupt a PDF on a
# platform where core.autocrlf is on.
*.pdf binary
